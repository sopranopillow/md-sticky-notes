// import * as rules from '../LanguageRules/MDRules.json';

const parseText = (text: string) => {
    // check for regex but save content, remove content and start.end then insert styled content.
    return '';
}

export default parseText;